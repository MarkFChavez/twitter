
<%= title "Home" %>

<div class="row-fluid">
	<div class="span4">
		<div class="top10 padding10" style="border:1px solid #CCC">
			<%= gravatar_for current_user, "pull-left", { size: 120 } %>
			<div>
				<span class="left10"> <%= current_user.email %> </span> <br />
				<span class="left10"> <%= link_to "change avatar", "http://gravatar.com/email", target: "_blank" %> <br />
					<span class="left10"> <%= link_to "logout", destroy_user_session_path, method: :delete %> </span> <br /> <br />
					<span class="left10 lead"> <%= pluralize current_user.microposts.count, "micropost" %> </span>	

			</div>
			<div class="clearfix"></div>
		</div>

		<div class="top10 padding10" style="border:1px solid #CCC">
			<div class="row-fluid">
				<div class="span12">
					<%= form_for(@micropost) do |f| %>
						<%= f.text_field :content, placeholder:"#your tweet", required:"required" %> <br /> <%= f.submit "Tweet", class: "btn" %>
					<% end %>
				</div>
			</div>

			<div class="row-fluid">
				<div class="span6">
					<span style="font-weight:bold">followers</span>
				</div>

				<div class="span6">
					<span style="font-weight:bold">following</span>
				</div>	
			</div>
		</div>
 	</div>

	<div class="span8">
		<div class="padding10 top10" style="border:1px solid #CCC">
			<div id="feed-header">
				<p class="lead" style="text-align:center"> News Feed </p>
			</div>

			<div id="feed-content">
				<% unless @microposts.empty? %>
					<% @microposts.each do |micropost| %>
						<div class="feed">
							<%= gravatar_for micropost.user, "pull-left", size: 80 %>
							<div style="margin-left:90px">
								<span class="label label-info"> <%= micropost.user.email %> </span> &nbsp; <span style="text-decoration:underline"> <%= time_ago_in_words(micropost.created_at) %> ago </span> &nbsp; 
								<% if can? :destroy, micropost %>
									<%= link_to "Delete", micropost, method: :delete, style:"color:red" %>
								<% end %>
								<br />
								<p style="text-align: justify"> <%= micropost.content %> </p>
							</div>
							<div class="clearfix"></div>
						</div>
					<% end %>
				<% else %>
					<p> No tweets yet. </p>
				<% end %>
			</div>
		</div>
	</div>
</div>

